swagger: '2.0'

info:
  version: "0.0.1"
  title: Academic Periods API

# Describe your paths here
paths:
  # This is a path endpoint. Change it.
  /academic-periods:
    # This is a HTTP operation
    get:
      tags:
       - academic-periods
      # Describe this verb here. Note: you can use markdown
      description: "Gets all `academic-level` objects. A combination of terms, years and subterms"
      operationId: getAcademicPeriods
      # This is array of GET operation parameters:
      produces:
        - application/json
      parameters:
        - in: query
          name: q
          description: "todo"
          required: false
          type: string
        - in: query
          name: page[size]
          description: "Number of results to return. Used in pagination. Default value is 10."
          required: false
          type: string
        - in: query
          name: page[number]
          description: "Page number of results. Used to paginate through results."
          required: false
          type: string
        - in: query
          name: category
          description: "The type of category: term, years and subterms."
          required: false
          type: string
      # Expected responses for this operation:
      responses:
        # Response code
        "200":
          description: "Successful response"
          schema:
            $ref: "#/definitions/ResultObjects"
        "404":
          description: "Not Found"
          schema:
            $ref: "#/definitions/Error"
        "500":
          description: "Internal Server Error"
          schema:
            $ref: "#/definitions/Errors"
      security:
        - locations_api_key: []

    

securityDefinitions:

  locations_api_key:
    type: apiKey
    name: locations_api_key
    in: query

definitions:
  ResultObject:
    properties:
      links:
        $ref: "#/definitions/Links"
      data:
        $ref: "#/definitions/ResourceObject"

  ResultObjects:
    properties:
      links:
        $ref: "#/definitions/Links"
      data:
        type: array
        items:
          $ref: "#/definitions/ResourceObject"
        
  ResourceObject:
    properties:
      id:
        type: string
      type:
        type: string
      attributes:
        $ref: "#/definitions/Attributes"
        
  Attributes:
    properties:
      abbreviation:
        type: string
      title:
        type: string
      startDate:
        type: string
        description: "The date when the period starts"
      endDate: 
        type: string
        description: "The date when the period ends"
      category:
        type: string
        description: "The category of the academic period (year, term or subterm)"

      
  Links:
    properties:
      self:
        type: string
      first:
        type: string
      last:
        type: string
      prev:
        type: string
      next:
        type: string

  Error:
    type: object
    properties:
      status:
        type: integer
        format: int32
      developerMessage:
        type: string
      userMesage:
        type: string
      code:
        type: integer
      details:
        type: string

  Errors:
    title: ArrayOfErrors
    type: array
    items:
      $ref: "#/definitions/Error"
